load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "loopal-ipc",
    srcs = glob(["src/**/*.rs"]),
    edition = "2024",
    proc_macro_deps = ["@crates//:async-trait"],
    visibility = ["//visibility:public"],
    deps = [
        "//crates/loopal-error",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tokio",
        "@crates//:tracing",
        "@crates//:uuid",
    ],
)

rust_test(
    name = "loopal-ipc-unit-test",
    crate = ":loopal-ipc",
    edition = "2024",
)

rust_test(
    name = "loopal-ipc_test",
    srcs = glob(["tests/**/*.rs"]),
    crate_root = "tests/suite.rs",
    data = ["//:loopal"],
    edition = "2024",
    env = {"LOOPAL_BINARY": "$(rlocationpath //:loopal)"},
    local = True,
    deps = [
        ":loopal-ipc",
        "//crates/loopal-agent-client",
        "//crates/loopal-protocol",
        "//crates/loopal-test-support",
        "@crates//:chrono",
        "@crates//:serde_json",
        "@crates//:tokio",
        "@crates//:uuid",
    ],
)
